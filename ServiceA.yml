greeting: Hello from ServiceA github 9:06

management:
  endpoints:
    web:
      exposure:
        include: '*'
    configprops:
      enabled: true
    beans:
      enabled: true
    shutdown:
      enabled: true

    enabled-by-default: true
    sensitive: false
    enabled: true

  endpoint:
    configprops:
      enabled: true
    beans:
      enabled: true
    shutdown:
      enabled: true
    health:
      sensitive: false
